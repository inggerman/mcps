apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcp-documentation
  namespace: mcps
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 2Gi
